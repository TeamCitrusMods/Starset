package dev.teamcitrusmods.starset.registry;

/*
Being saved for 1.20
 */
//@Mod.EventBusSubscriber(modid = FundamentalsIncognita.MODID, bus = Mod.EventBusSubscriber.Bus.MOD)
public class StarsetModCreativeTab {
    /*
    @SubscribeEvent
    public static void registerCreativeTabs(CreativeModeTabEvent.Register event) {
        event.registerCreativeModeTab(new ResourceLocation(FundamentalsIncognita.MODID, "tab"), builder -> builder.title(Component.translatable("itemGroup.fundamentalsIncognita"))
                .icon(() -> new ItemStack(Items.ACACIA_BOAT))
                .displayItems((feature, output, hasPerm) -> FIModItems.ITEMS.getEntries().forEach(i -> output.accept(i.get()))));
    }

     */
}
